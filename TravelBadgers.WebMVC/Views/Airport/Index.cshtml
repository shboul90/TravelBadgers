@model IEnumerable<TravelBadgers.Models.AirportListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AirportId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AirportName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AirpotZipCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AirportId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AirportName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AirpotZipCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AirportId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AirportId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AirportId })
        </td>
    </tr>
}

</table>
